cmake_minimum_required(VERSION 3.10)
project(most_calories)
enable_language(Fortran)
add_executable(most_calories most_calories.f90)
target_compile_options(most_calories PRIVATE -Wall)
set(CMAKE_BUILD_TYPE Release)
